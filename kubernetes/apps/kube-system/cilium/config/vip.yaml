---
apiVersion: v1
kind: Service
metadata:
  name: kube-vip
  annotations:
    external-dns.alpha.kubernetes.io/hostname: k8s.youmans.io
    lbipam.cilium.io/ips: 10.0.2.2
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    k8s-app: kube-apiserver
    tier: control-plane
  ports:
    - port: 6443
